package org.example.Tdd.exercices;
/*
import org.example.Tdd.exercices.GradingCalculator;
import org.junit.Assert;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.Arguments;
import org.junit.jupiter.params.provider.CsvSource;

import java.util.stream.Stream;*/

public class GradingCalculatorTest {

   /* @ParameterizedTest
    //@MethodSource("provideArguments")
    @CsvSource({
            "95, 90, A",
            "85, 90, B",
            "65, 90, C",
            "95, 65, B",
            "95, 55, F",
            "65, 55, F",
            "50, 90, F",
    })
    public void shouldReturnCorrectGrade(int score, int presence, char result) {
        System.out.println("Test avec score " + score + " et presence " + presence + " renvoie " + result);
        GradingCalculator gradingCalculator1 = new GradingCalculator(score, presence);
        char test = gradingCalculator1.getGrade();
        Assert.assertEquals(result, test);
    }

    private static Stream<Arguments> provideArguments() {
        return Stream.of(
                Arguments.of(95, 90, 'A') ,
                Arguments.of(85, 90, 'B') ,
                Arguments.of(65, 90, 'C') ,
                Arguments.of(95, 65, 'B') ,
                Arguments.of(95, 55, 'F') ,
                Arguments.of(65, 55, 'F') ,
                Arguments.of(50, 90, 'F')
        );
    }*/

}

